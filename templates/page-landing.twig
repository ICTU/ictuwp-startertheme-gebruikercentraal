{% extends 'single.twig' %}



  {% block post_classlist %}
<article
    class="post post--full post--type-landing {{ is_gutenberg == 'TRUE' ? 'post--gutenberg' : 'post--plain' }}{{ has_comments ? ' ' ~ 'l-has-comments' }}"
    id="post-{{ post.ID }}">
  {% endblock %}

