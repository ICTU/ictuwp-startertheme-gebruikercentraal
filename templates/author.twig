{% extends "base.twig" %}

          {% if author.meta('auteursfoto_url') is not empty %}
            {% include 'sections/section-authorbox.html.twig'%}
          {% endif %}

{% block content %}
	{% for post in posts %}
		{% include ["tease-"~post.post_type~".twig", "tease.twig"] %}
	{% endfor %}
{% endblock %}
