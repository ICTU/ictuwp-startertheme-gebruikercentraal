<div class="section section--actual">

  <div class="l-section-content">

    {# Calendar col #}
    <section class="l-section-col col--50-width">
      {% if items.events.title %}
        <h2 class="section__title">{{ items.events.title }}</h2>
      {% endif %}

      <div class="grid grid--col-2">

        {% for event in items.events.items %}

          {% include 'components/teaser-small.html.twig' with {
            'title': event.title,
            'url': event.url,
            'img': event.img,
            'alt': event.img_alt,
            'date': event.start_date,
            'type': 'event'
          } %}

        {% endfor %}
      </div>

      {% if items.events.cta %}
        <a class="btn btn--primary" href="{{ items.events.cta.url }}">{{ items.events.cta.title }}</a>
      {% endif %}

    </section>

    {# Blog / news col #}
    <section class="l-section-col col--50-width">
      <h2 class="section__title">{{ items.blogs.title ? items.blogs.title : 'Blogs' }}</h2>

      <div class="grid grid--col-2">

        {% for blog in items.blogs.items %}
          {% include 'components/teaser-small.html.twig' with {
            'title': blog.title,
            'url': blog.url,
            'img': blog.img,
            'alt': blog.img_alt,
            'type': 'blog'
          } %}

        {% endfor %}
      </div>

      {% if items.blogs.cta %}
        <a class="btn btn--primary" href="{{ items.blogs.cta.url }}">{{ items.blogs.cta.title }}</a>
      {% endif %}
    </section>


  </div>
</div>
